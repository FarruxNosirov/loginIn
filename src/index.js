import React from "react";
import ReactClintDOM from "react-dom/client";
const container = document.getElementById("root");

const root = ReactClintDOM.createRoot(container);
root.render(<App />);
import App from "./App";
import "./index.css";
